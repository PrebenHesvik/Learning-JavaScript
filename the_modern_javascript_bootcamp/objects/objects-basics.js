// object
let myBook = {
  title: "A New Earth",
  author: "Eckhart Tolle",
  pageCount: 350
};

// access class properties
let book = `${myBook.title} by ${myBook.author}`;
console.log(book);

// Change a property of the class
myBook.pageCount = 360;
